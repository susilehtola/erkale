add_executable (erkale_co-optimize co-optimize.cpp)
set_target_properties(erkale_co-optimize PROPERTIES OUTPUT_NAME "erkale_co-optimize${SUFFIX}")
target_link_libraries(erkale_co-optimize liberkale_cmp)
target_link_libraries(erkale_co-optimize liberkale_emd)
target_link_libraries(erkale_co-optimize liberkale)
# Link other libraries if necessary
if(BUILD_SHARED_LIBS)
else()
 target_link_libraries(erkale_co-optimize ${GSL_LIBRARIES})
 target_link_libraries(erkale_co-optimize ${LAPACK_LIBRARIES})
 target_link_libraries(erkale_co-optimize ${LIBINT_LIBRARIES})
 target_link_libraries(erkale_co-optimize ${LIBXC_LIBRARIES})
 target_link_libraries(erkale_co-optimize ${HDF5_LIBRARIES})
endif()
